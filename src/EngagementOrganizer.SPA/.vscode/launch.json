{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start with debugging",
            "type": "edge",
            "request": "launch",
            "url": "http://localhost:4200",
            "preLaunchTask": "ngserve",
            "webRoot": "${workspaceRoot}"            
        }
    ]
}